import{_ as a,o as s,c as t,ae as i}from"./chunks/framework.1HOg4yJX.js";const u=JSON.parse('{"title":"The Knowledge Graph — Semantic Memory as a Queryable Ledger","description":"","frontmatter":{},"headers":[],"relativePath":"KnowledgeGraph.md","filePath":"KnowledgeGraph.md"}'),o={name:"KnowledgeGraph.md"};function n(r,e,h,l,d,c){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="the-knowledge-graph-—-semantic-memory-as-a-queryable-ledger" tabindex="-1">The Knowledge Graph — Semantic Memory as a Queryable Ledger <a class="header-anchor" href="#the-knowledge-graph-—-semantic-memory-as-a-queryable-ledger" aria-label="Permalink to &quot;The Knowledge Graph — Semantic Memory as a Queryable Ledger&quot;">​</a></h1><p><strong>neurons.me / suiGn</strong><strong>Status:</strong> Describes the current <code>monad.ai</code> implementation (semantic memory log, <code>appendSemanticMemory</code>, and the host-telemetry bridge). Not a wire protocol — see <a href="./NRP-v0.3.0.html">NRP v0.3.0</a> for how a <code>me://</code> target actually gets resolved across the mesh. <strong>License:</strong> CC0 1.0 Universal — Public Domain</p><hr><h2 id="_0-the-one-sentence-version" tabindex="-1">0. The one-sentence version <a class="header-anchor" href="#_0-the-one-sentence-version" aria-label="Permalink to &quot;0. The one-sentence version&quot;">​</a></h2><p>Every fact a monad writes into its kernel through <code>appendSemanticMemory</code> becomes a node in an append-only, hash-chained, path-addressable graph — resolvable through the exact same <code>me://</code> machinery as everything else in <code>.me</code>, with no separate registration step. There is no distinct &quot;knowledge graph database.&quot; The knowledge graph <em>is</em> the kernel&#39;s semantic memory log, read back.</p><h2 id="_1-two-kinds-of-kernel-writes-—-only-one-is-a-graph-node" tabindex="-1">1. Two kinds of kernel writes — only one is a graph node <a class="header-anchor" href="#_1-two-kinds-of-kernel-writes-—-only-one-is-a-graph-node" aria-label="Permalink to &quot;1. Two kinds of kernel writes — only one is a graph node&quot;">​</a></h2><p><code>monad.ai</code>&#39;s HTTP layer has two ways to put a value into the kernel, and they are not interchangeable:</p><table tabindex="0"><thead><tr><th></th><th><code>kernelSet</code></th><th><code>appendSemanticMemory</code></th></tr></thead><tbody><tr><td>Where it lives</td><td>Raw <code>.me</code> proxy chain (<code>kernel.daemon.claims.foo(value)</code>)</td><td>The replayable, hash-chained memory log</td></tr><tr><td>Visible to <code>listSemanticMemoriesByNamespace</code> / <code>me://</code> reads</td><td>No</td><td>Yes</td></tr><tr><td>Used for</td><td>Internal daemon bookkeeping (<code>daemon.claims.*</code>, <code>daemon.users.*</code>)</td><td>Anything meant to be queried later</td></tr></tbody></table><p>A fact written with <code>kernelSet</code> exists, but it is invisible to the knowledge graph — it never appears in a namespace&#39;s memory log, never gets a hash, never shows up in a <code>/blockchain</code> listing. A fact written with <code>appendSemanticMemory</code> does all three. If you want something to be <em>knowledge</em> — resolvable, queryable, ordered, hash-chained — it has to go through <code>appendSemanticMemory</code>, not <code>kernelSet</code>.</p><h2 id="_2-what-queryable-actually-means-here" tabindex="-1">2. What &quot;queryable&quot; actually means here <a class="header-anchor" href="#_2-what-queryable-actually-means-here" aria-label="Permalink to &quot;2. What &quot;queryable&quot; actually means here&quot;">​</a></h2><p>Once a value is written via <code>appendSemanticMemory({ namespace, path, data })</code>, it is reachable two ways, both already generic — neither requires registering the new path anywhere:</p><p><strong>By exact path</strong>, through the same <code>me://</code> resolution every other semantic value uses:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /apps/&lt;name&gt;/surface/host/cpu   (HTTP, path segments = dots)</span></span>
<span class="line"><span>me://&lt;namespace&gt;:read/surface.host.cpu   (NRP form)</span></span></code></pre></div><p><strong>By listing</strong>, through the namespace&#39;s own memory log (what a &quot;Blockchain&quot; tab in a UI renders):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /apps/&lt;name&gt;/blockchain?prefix=surface.host</span></span></code></pre></div><p>Both paths resolve through <code>namespaceToKernelPrefix()</code> first — write and read under the namespace&#39;s <em>canonical</em> form (its actual root namespace, e.g. <code>suis-macbook-air.local</code>), not a gateway-routing alias (<code>local.host</code>, <code>local.netget</code>). Those aliases get translated to the canonical namespace by the HTTP layer before either read or write ever reaches the kernel; they are addressing conveniences, not namespaces themselves, and writing under the alias directly would silently land in the wrong place (or nowhere resolvable at all).</p><h2 id="_3-existing-precedent-surface-usage" tabindex="-1">3. Existing precedent: <code>surface.usage.*</code> <a class="header-anchor" href="#_3-existing-precedent-surface-usage" aria-label="Permalink to &quot;3. Existing precedent: \`surface.usage.*\`&quot;">​</a></h2><p>Before host telemetry, exactly one thing already bridged live, in-memory metrics into the graph this way: <code>resources/usageLedger.ts</code>&#39;s <code>ResourceUsageLedger</code>. It listens for HTTP request events and periodically writes two things under the root namespace:</p><ul><li><code>surface.usage.requests</code> — one entry per HTTP request (method, url, status, duration, identityHash)</li><li><code>surface.usage.window</code> — a 10-second rolling aggregate (request rate, CPU ratio)</li></ul><p>Every entry carries an Ed25519 signature (<code>sig: { alg, value, pubKey }</code>), deterministically derived from the monad&#39;s own <code>SEED</code> via HKDF — because this feed exists to support &quot;resource crypto&quot;: NetGet and downstream accounting consumers need to trust these numbers enough to bill against them. Signing is what makes that trust possible without a central authority re-verifying the daemon&#39;s honesty.</p><h2 id="_4-host-telemetry-the-second-bridge" tabindex="-1">4. Host telemetry: the second bridge <a class="header-anchor" href="#_4-host-telemetry-the-second-bridge" aria-label="Permalink to &quot;4. Host telemetry: the second bridge&quot;">​</a></h2><p><code>resources/hostTelemetryLedger.ts</code> follows the identical shape — a <code>start()</code>/<code>stop()</code> timer, sampling live system state and writing it into the same graph — but for a different kind of fact: not &quot;what happened,&quot; but &quot;what does this machine currently look like.&quot;</p><p>Every 30 seconds (configurable via <code>MONAD_HOST_TELEMETRY_INTERVAL_MS</code>), it samples and writes three paths under the root namespace:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>surface.host.cpu      { cores, usageRatio, timestamp }</span></span>
<span class="line"><span>surface.host.memory   { totalGb, usageRatio, source, usedGb, reclaimableGb, breakdown, timestamp }</span></span>
<span class="line"><span>surface.host.storage  { totalGb, usageRatio, timestamp }</span></span></code></pre></div><h3 id="_4-1-why-memory-pressure-needed-its-own-measurement" tabindex="-1">4.1 Why memory pressure needed its own measurement <a class="header-anchor" href="#_4-1-why-memory-pressure-needed-its-own-measurement" aria-label="Permalink to &quot;4.1 Why memory pressure needed its own measurement&quot;">​</a></h3><p>The naive Node.js approach — <code>(os.totalmem() - os.freemem()) / os.totalmem()</code> — reads as 90%+ &quot;used&quot; on almost any macOS machine at rest, because macOS deliberately fills &quot;free&quot; RAM with reclaimable disk cache rather than reporting it as free. That number is not wrong, exactly — it is answering a different question (&quot;how much RAM is not instantly zero&quot;) than the one that actually matters (&quot;is this machine under memory pressure&quot;).</p><p><code>surface.host.memory.source</code> reports which measurement was used:</p><ul><li><strong><code>vm_stat</code></strong> (macOS) — parses <code>vm_stat</code>&#39;s page categories directly: <code>wired + active + compressed</code> counts as real, pinned usage; <code>inactive + speculative + free</code> counts as instantly reclaimable. This is the same split macOS&#39;s own Activity Monitor &quot;Memory Pressure&quot; gauge is built from. <code>breakdown</code> carries all six categories in GB.</li><li><strong><code>proc_meminfo</code></strong> (Linux) — reads <code>/proc/meminfo</code>&#39;s <code>MemAvailable</code> field directly (kernel-computed, accounts for reclaimable page cache/slab since Linux 3.14 — no need to hand-roll the equivalent split).</li><li><strong><code>naive</code></strong> — the plain total-minus-free fallback, used only when the platform-specific measurement isn&#39;t available (any other OS, or if <code>vm_stat</code>/<code>/proc/meminfo</code> fails to parse).</li></ul><h3 id="_4-2-why-this-bridge-is-unsigned" tabindex="-1">4.2 Why this bridge is unsigned <a class="header-anchor" href="#_4-2-why-this-bridge-is-unsigned" aria-label="Permalink to &quot;4.2 Why this bridge is unsigned&quot;">​</a></h3><p><code>surface.usage.*</code>&#39;s signature exists because that feed backs billing. <code>surface.host.*</code> is self-reported observability — &quot;here is what this machine says about itself,&quot; the same trust tier as <code>claimSemantics.ts</code>&#39;s plain seed writes (also unsigned). If a future use case needs to <em>bill</em> against reported hardware capacity, that would warrant a <code>measured</code>/<code>verified</code> tier distinct from what a host merely reports about itself — a real, separate hardware attestation problem, not solved by adding a signature to a self-report.</p><h2 id="_5-reading-it-back" tabindex="-1">5. Reading it back <a class="header-anchor" href="#_5-reading-it-back" aria-label="Permalink to &quot;5. Reading it back&quot;">​</a></h2><p>Any client — a browser, a CLI, another monad — reads these the same way it reads anything else in the graph, no special-case client code:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">gatewa</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/apps/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">nam</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/surface/host/cpu</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;cores&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;usageRatio&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.53</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;timestamp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1787915193156</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;disclosure&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;public&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>this.gui</code>&#39;s <code>HostSurface</code> component (<code>packages/GUI/Typescript/src/gui/All.This/Host/HostSurface.tsx</code>) is one such client — it polls the live <code>/__bootstrap</code> surface entry (which already carries the same computed ratios merged in) for its gauges, rather than reading back through the graph on every render. The graph write exists so the <em>history</em> survives past that one HTTP response — so a later query, another surface, or a future aggregation can see what this host reported an hour or a week ago, not just what it reports right now.</p><h2 id="_6-what-this-is-not" tabindex="-1">6. What this is not <a class="header-anchor" href="#_6-what-this-is-not" aria-label="Permalink to &quot;6. What this is not&quot;">​</a></h2><ul><li>Not a new database. There is exactly one storage mechanism: the kernel&#39;s own hash-chained memory log, the same one <code>users.*</code> and <code>daemon.claims.*</code> already live in.</li><li>Not a wire protocol change. Reading <code>surface.host.*</code> uses the same <code>me://</code> resolution and HTTP path-resolver every other semantic path already uses — see <a href="./NRP-v0.3.0.html">NRP v0.3.0</a> for that layer.</li><li>Not verified or attested capacity. See §4.2 — this is self-report, not certification.</li></ul><h2 id="see-also" tabindex="-1">See also <a class="header-anchor" href="#see-also" aria-label="Permalink to &quot;See also&quot;">​</a></h2><ul><li><a href="./NRP-v0.3.0.html">NRP v0.3.0</a> — how a <code>me://</code> target is resolved across the mesh; this document only covers what happens once a value is already in one kernel&#39;s own memory log.</li><li><a href="./Mesh/status.html">Mesh/status.md</a> — implementation status of the broader mesh, separate from this bridge.</li><li><code>resources/usageLedger.ts</code> — the signed, billing-facing sibling of this bridge.</li></ul>`,39)])])}const m=a(o,[["render",n]]);export{u as __pageData,m as default};
