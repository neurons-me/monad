name: Deploy monad.ai Docs

on:
  push:
    branches: [main]
    paths:
      - "Typescript/docs/**"
      - "Typescript/src/**"
      - "Typescript/typedoc.json"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      # Install VitePress from repo root — avoids npm/package.json which contains
      # workspace:* deps that npm cannot resolve (workspace: is pnpm-only syntax).
      # TypeDoc API markdown (docs/api/) is committed, so no typedoc run needed.
      - name: Install VitePress
        run: npm install --no-save vitepress

      - name: Build VitePress
        run: npx vitepress build Typescript/docs
        env:
          VITEPRESS_BASE: /monad/Typescript/typedocs/

      - name: Add .nojekyll (prevents Jekyll from processing VitePress output)
        run: mkdir -p Typescript/typedocs && touch Typescript/typedocs/.nojekyll

      # Deploys to the gh-pages branch under Typescript/typedocs/.
      # Files outside Typescript/typedocs/ in gh-pages are preserved (keep_files: true).
      # GitHub Pages source: Branch: gh-pages  /  Folder: / (root)
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./Typescript/typedocs
          destination_dir: Typescript/typedocs
          keep_files: true
